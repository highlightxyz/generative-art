# generative-art
## Script
`hl-gen-art-v1.js` script is needed to create generative art on the highlight platform.

### TODO
1. Add `typescript` version of the script.
2. Add `examples` directory.
3. Convert script into publishable npm package.
